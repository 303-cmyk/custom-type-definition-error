# Why does this yield a runtime error?

## Installation

```
git clone https://www.github.com/dschuessler/custom-type-definition-error
cd custom-type-definition-error
npm install
```

## Run

```
npx tsc -p . && node built/index.js
```